package rocks.zipcode.io.quiz3.fundamentals;

/**
 * @author leon on 09/12/2018.
 */
public class PigLatinGenerator {
    public String translate(String str) {
        /*
        StringBuilder pig = new StringBuilder(str);
        StringBuilder latin = new StringBuilder();

        str.split(" ");
        for(int i = 0; i < str.length(); i ++){
            if(pig.charAt(i) != ' '){
                latin.append(pig.charAt(i));
                pig.

            }
        }

        pig.insert(str.length(), pig.charAt(0));
        pig.replace(0,1, "");
        pig.insert(str.length(), 'a');
        pig.insert(str.length(), 'y');



        return pig.toString();
    }

         */
        return null;
    }
}